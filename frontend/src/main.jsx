import React from 'react';
import ReactDOM from 'react-dom/client';
import { PhotoDisplayApp } from './PhotoDisplayApp';
import './styles.css';
import './normalize.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <PhotoDisplayApp />
  </React.StrictMode>
);
